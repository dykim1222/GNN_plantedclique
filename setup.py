from distutils.core import setup

setup(
        install_requires = ['torch==0.4.0',
                            'visdom',
                            'matplotlib',
                            'tensorflow==1.7.0'
                            ]
    )
